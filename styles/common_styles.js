import { StyleSheet } from "react-native/types";

export const styles = StyleSheet.create({
    rowStyles:{
        display: 'flex',
        flexDirection: 'row'
    },
    colStyles:{
        display: 'flex',
        flexDirection: 'column'
    }
})
